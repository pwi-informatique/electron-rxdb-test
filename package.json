{
  "name": "electron-rxdb-test",
  "main": "main.js",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npm run electron",
    "start:custom": "npm run electron:custom",
    "electron": "electron . -enable-logging",
    "electron:custom": "electron . -enable-logging --custom"
  },
  "dependencies": {
    "electron": "^27.1.3",
    "rxdb": "^14.17.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "playwright-core": "1.40.1"
  }
}
